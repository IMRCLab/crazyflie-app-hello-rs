# This doesn't work
# obj-y += cfapprs-module.o
# cfapprs-module-objs += cfapprs.o build/libcfapprs.a

# This also doesn't work
lib-y += target/thumbv7em-none-eabihf/release/libcfapprs.a

# This also doesn't work
#obj-y += build/libcfapprs.a
